import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const Services = () => {
  return (
    <SafeAreaView>
      <View className="px-4">
        <Text className="font-bold text-xl">Referrals</Text>
      </View>
    </SafeAreaView>
  );
};

export default Services;
